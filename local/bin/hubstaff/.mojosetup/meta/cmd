#!/bin/sh
#---------------------------------------------
#   helper to run a command and get its result
#
#   Mojo only supports running scripts from the meta dir
#   It does not support running arbitrary commands and 
#   getting the exit code
#
#---------------------------------------------

echo "Launch: $@"

"$@"
